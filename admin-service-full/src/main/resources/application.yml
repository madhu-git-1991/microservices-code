spring:
  application:
    name: admin-service

  datasource:
    url: jdbc:mysql://localhost:3306/admin_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8101

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    root: INFO

# Resource server JWT settings - replace jwk-set-uri with auth server URI
#spring:
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          jwk-set-uri: http://localhost:9000/.well-known/jwks.json
