spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookingdb
    username: root
    password: rootroot

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8381

#spring:
#  activemq:
#    broker-url: tcp://activemq:61616
#    user: admin
#    password: admin
#    in-memory: false